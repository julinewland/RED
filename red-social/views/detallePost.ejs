<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>The Network</title>
    <link rel="icon"  href="/images/favicon.ico" type="image/icon type">
    <link href="https://fonts.googleapis.com/css2?family=Rubik:wght@400;500;600;700;800;900&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Nunito:wght@200;300;400;600;700;800;900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/stylesheets/detallePost.css">
    <link rel="stylesheet" href="/stylesheets/header.css">
    <link rel="stylesheet" href="/stylesheets/general.css">
    <link rel="stylesheet" href="/stylesheets/footer.css">

    <%- include("links")%>
    
</head>

<body>
    <%- include("header")%>
    <main class="main"> 
        <div class="uk-section uk-section-xsmall">
            <div class="uk-container uk-container-large uk-flex uk-flex-middle uk-flex-center postdetalle" >
                <section class="imagen uk-flex uk-width-2-3 detalle-container">
                    <article class="uk-width-2-3 uk-flex uk-flex-center uk-flex-middle detalle-post">
                        <img src="<%=post.urlImagen%>" alt="Imagen del post" class="imagen-post ">
                    </article>
                    <article class="uk-width-1-3 uk-flex uk-flex-column comentarios-detalle">
                        <div class="foto-perfil info-post uk-flex uk-flex-middle">
                            <img src="<%=post.usuarioPost.fotoDePerfil%>" alt="Foto del usuario">
                            <h3><%=post.usuarioPost.nombre%></h3>
                            <% if (usuarioLog != undefined) { %>
                                <% if (usuarioLog.id == post.idusuario) { %>
                                <form action="/post/borrar" method="POST">
                                    <input type="hidden" name="idPost" value="<%=post.id%>">
                                    <input type="hidden" name="idUsuario" value="<%=post.idusuario%>">
                                    <button type="submit" class="boton1"> Borrar Post</button>
                                </form>
                                <a href="/post/editar/<%=post.id%>" class="link">Editar Post</a>
                            <% } }%>
                        </div>
                        
                        <div class="lista-comentarios" >
                        <%for (let i = 0; i < post.coment.length; i++) { %>
                                <div class="comentario uk-flex uk-flex-middle uk-margin-small-top">
                                    <div class="uk-width-1-4 uk-flex uk-flex-center">
                                        <img src="<%=post.coment[i].usuarioCom.fotoDePerfil%>" alt="Foto de usuario">
                                    </div>
                                    <h6 class="uk-margin-remove uk-width-2-4"><a href="/user/detalle/<%=post.coment[i].idusuario%>"><%=post.coment[i].usuarioCom.nombre%></a> <%=post.coment[i].texto%></h6>
                                </div>
                        <%}%>
                        
                     </div>
                    </article>
                </section>
            </div>
        </div>
    </main>
    <%- include("footer")%>
</body>

</html>